# 🎙️ AI 配音助手 Pro - 2.0 旗舰版项目地图

此文档记录了当前项目的实际文件结构及其功能定义，供日后维护与二次开发参考。

---

## 📁 核心目录结构

### 1. 样式与全局配置 (`/src/app`)
- **`globals.css`**: 全局视觉灵魂。定义了品牌紫 (`#9C27B0`)、拟态阴影 (`shadow-physical`)、内阴影及所有动态动画（如声波、闪烁效果）。
- **`layout.tsx`**: 项目根布局。负责全局状态包裹及基础 HTML 结构。
- **`page.tsx`**: 主落地页。组合了导航栏、侧边栏、编辑器和控制面板的总闸。
- **`api/dubbing/route.ts`**: 后端核心。处理流式音频请求，对接语音引擎，管理 SSML 转换逻辑。

### 2. 界面组件库 (`/src/components`)
- **`Navbar.tsx`**: 顶部导航。包含菜单映射、个人中心显示及“商业授权”状态标签。
- **`VoiceSidebar.tsx`**: 音色资源库。处理 `320px` 侧边栏布局、毛玻璃效果及音色选中逻辑。
- **`VoiceCard.tsx`**: 音色卡片。渲染单个音色的头像（带内阴影）、名称及适用场景。
- **`EditorArea.tsx`**: 灵感编辑器。包含拟态输入框、情感标签插入逻辑及字数进度条。
- **`ControlPanel.tsx`**: 生成控制台。负责点击生成、加载状态显示（声波动画）及音频播放/下载。
- **`VisitorStats.tsx`**: 数据展示。统计并显示用户访问或使用的相关数据。

### 3. 数据与逻辑管理
- **`src/constants/voicesData.ts`**: 静态数据库。管理所有音色的配置信息（ID、性别、渐变色、场景）。
- **`src/hooks/useDubbing.ts`**: 业务钩子。封装了音频请求的逻辑，实现 UI 与请求的分离。
- **`tailwind.config.ts`**: 框架扩展。定义了项目中使用的特定圆角、投影及颜色变量。

### 4. 静态资源 (`/public`)
- **`avatars/` & `previews/`**: 存放音色头像图片与预设试听音频。
- **`SSML_bgm.wav`**: 默认背景音乐资源。
- **各种图标**: `file.svg`, `globe.svg`, `support.png` 等 UI 装饰元素。

---

## 🛠️ 快速维护指南

- **添加新音色**：只需修改 `voicesData.ts` 数组，前端会自动同步头像和卡片。
- **调整视觉质感**：若需修改拟物感的深浅，请在 `globals.css` 中搜索 `.shadow-physical` 或 `shadow-inner-physical`。
- **修改字数上限**：在 `EditorArea.tsx` 中搜索 `MAX_LIMIT` 常量。
- **更改 API 参数**：前往 `api/dubbing/route.ts` 调整与后端引擎对接的参数。

---
*最后更新日期：2026-05-08*